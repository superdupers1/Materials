@model IEnumerable<Materials.Models.PartNumbers>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink("/ Download to Excel", "DownloadExcel")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PartNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Available)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customers.Customer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PartNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Available)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customers.Customer)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PKPartNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.PKPartNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PKPartNumber })
        </td>
    </tr>
}

</table>
